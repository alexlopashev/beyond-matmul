# Evidence Matrix

This matrix maps the current whitepaper claims to the executable evidence or
explicit boundary that backs them. It is intentionally compact: it should point
reviewers to the source of truth, not duplicate every test assertion.

## Claim Map

| Claim area | Current claim | Evidence | Boundary |
| --- | --- | --- | --- |
| Project scope | Beyond Matmul targets fixed-weight inference where dense GEMM remains a valid fallback. | `README.md`, `whitepaper/main.tex`, `docs/research_outline.md` | Training-time mutation, production GPU kernels, and hardware-calibrated speedup claims remain out of scope. |
| Torch frontend coverage | The Torch FX frontend preserves supported fixed-weight linear, adapter, embedding-projection, Conv1d including scalar stride/padding/dilation and grouped/depthwise forms, matmul/mm, addmm, and per-tensor affine quantized `nn.Linear` patterns before densification. | `docs/torch_frontend_coverage.md`; `tests/test_frontend.py` quantized linear and real grouped/depthwise Conv1d cases; `examples/torch_fx_frontend_demo.py`; `examples/torch_coverage_demo.py`; `examples/adapter_workload_demo.py`; `examples/conv1d_workload_demo.py`; `tests/test_case_study_artifacts.py` | Multi-dimensional Conv1d parameter shapes, invalid Conv1d output lengths, Conv2d, quantized convolution, per-channel/per-axis or dynamic quantized modules, broader exported graph variants, and dynamic-weight matmul/addmm are listed as unsupported, not claimed as covered. |
| IR operator families | The IR has executable semantics and dense fallbacks for dense, diagonal, sparse COO, fixed-mask, low-rank, affine, codebook, bitpacked binary, packed affine quantized weights, single-channel Conv1d, and multi-channel Conv1d operators including scalar stride/padding/dilation and grouped/depthwise channel partitions. The quantized fixed-weight contract distinguishes exact codebook, tensor-wide scaled binary, and per-tensor affine integer payloads, approximate candidates, dense dequantization fallback, and unsupported per-axis cases. | `docs/ir_spec.md`; `beyond_matmul/ir.py`; `tests/test_ir_planner.py::test_structured_operators_match_dense_application`; `tests/test_ir_planner.py::test_fixed_mask_operator_matches_dense_application`; `tests/test_ir_planner.py::test_packed_affine_quantized_operator_matches_dense_dequantization`; `tests/test_ir_planner.py::test_packed_affine_quantized_operator_validates_payload_metadata`; `tests/test_ir_planner.py::test_planner_keeps_packed_affine_quantized_lowering_and_dense_fallback`; `tests/test_ir_planner.py::test_conv1d_to_dense`; `tests/test_ir_planner.py::test_strided_padded_and_dilated_conv1d_to_dense`; `tests/test_ir_planner.py::test_multi_channel_conv1d_to_dense`; `tests/test_ir_planner.py::test_grouped_conv1d_to_dense`; `tests/test_ir_planner.py::test_depthwise_conv1d_to_dense`; `tests/test_ir_planner.py::test_strided_padded_dilated_grouped_conv1d_to_dense` | Dense fallback is preserved as an equivalence and portability path. Fixed-mask support covers explicit binary masks applied as linear maps, not score-dependent masked attention. This is not a claim that every backend has specialized kernels or that every Torch quantized module is captured today. |
| Recovery after lost provenance | Cheap analyzers can identify selected dense-matrix structures and expose bounded heuristic confidence, reuse evidence, and output-relative validation evidence when sample inputs are supplied. | `beyond_matmul/analyzer.py`; `tests/test_analyzer.py`; `examples/fixed_weight_inference_demo.py`; `docs/research_outline.md` | Recovery is weaker than preserved provenance. Confidence is not statistically calibrated, validation is limited to the supplied samples, and broader recovery quality should remain future work until benchmarked. |
| Planner exactness and fallback | The planner selects valid lowerings under exactness, error, reuse, backend, and layout contracts, with dense GEMM as fallback. A deterministic ablation compares exact-only versus bounded-error requests on the same case, rejects a preprocessing-heavy lowering before its reuse threshold, accepts it at the threshold, and rejects an unsupported codebook lowering on GPU while preserving dense fallback. | `beyond_matmul/planner.py`; `docs/ir_spec.md`; `tests/test_ir_planner.py::test_planner_selects_diagonal_kernel_for_diagonal_weight`; `tests/test_ir_planner.py::test_planner_selects_fixed_mask_sparse_lowering`; `tests/test_ir_planner.py::test_planner_uses_dense_fallback_when_error_contract_disallows_approximation`; `tests/test_ir_planner.py::test_planner_preserves_affine_low_rank_bias`; `tests/test_ir_planner.py::test_planner_preserves_affine_multi_channel_conv1d_bias`; `benchmarks/planner_contract_ablation.py`; `tests/test_planner_contract_ablation.py`; `docs/benchmark_artifacts.md` | Cost choices are model estimates unless backed by the benchmark artifacts below. The planner-contract ablation is deterministic coverage, not hardware performance evidence. |
| Approximation and error contracts | Approximate low-rank, sparse top-k, codebook, and bitpacked candidates are evaluated with product-aware/output-relative error, not only matrix reconstruction error. | `beyond_matmul/approximations.py`; `tests/test_ir_planner.py::test_product_error_contract_can_accept_low_rank`; `benchmarks/approximation_error_ablation.py`; `tests/test_approximation_error_ablation.py`; `docs/benchmark_artifacts.md` | The current ablation is one deterministic synthetic case. It supports the need for output-aware acceptance, not broad model-quality conclusions. |
| Benchmark and cost claims | The project records operation, memory, preprocessing, amortization, output-error, and pure-Python latency proxies for controlled fixed-weight cases, and the whitepaper summarizes only fields present in the generated artifacts. The PEFT capstone artifact from issue #80/PR #90 records a real upstream-vs-fork matrix: seq16 and seq64 correctness pass for upstream unmerged PEFT, upstream merged dense, and the Beyond Matmul PEFT fork; seq128 fails across all baselines, so `summary.benchmark_ready` is false and `summary.performance_claim` is `none`. | `whitepaper/main.tex` current benchmark results; `benchmarks/fixed_weight.py`; `docs/results/fixed_weight.json` workflow artifact; `benchmarks/planner_contract_ablation.py`; `docs/results/planner_contract_ablation.json` workflow artifact; `benchmarks/peft_transformers_lora_inference.py`; `docs/results/peft_transformers_lora_inference.json`; `docs/benchmark_artifacts.md`; `tests/test_benchmark_artifacts.py`; `tests/test_planner_contract_ablation.py`; `tests/test_peft_transformers_lora_inference.py`; `tests/test_ci_local.py`; `.github/workflows/ci.yml`; `scripts/ci_local` | Timings are pure-Python research proxies and figure-generation inputs unless marked as a real benchmark artifact. The PEFT capstone run is measured local CPU evidence, but it is a negative/blocked result with no hardware speedup claim, no measured adapter-switching cost, and no CPU peak-memory measurement. Planner-contract costs are estimates, not runtime measurements. |
| External PEFT provenance | The measured PEFT plus Transformers capstone shows that the fork can expose structured LoRA provenance events for successful seq16/seq64 fixed-adapter inference rows while keeping dense fallback available and matching upstream outputs. | `docs/results/peft_transformers_lora_inference.json`; `docs/benchmark_artifacts.md`; `benchmarks/peft_transformers_lora_inference.py`; `docs/peft_capstone_benchmark_contract.md`; `docs/peft_low_rank_provenance_design.md`; `tests/test_peft_transformers_lora_inference.py`; PEFT fork branch `alexlopashev/peft@beyond-matmul/provenance-lora-inference` at `7ac8d57b100846837c5a3b76c65e1e1954ccc3c8` | This evidence is limited to the contract-defined CPU fp32 prefill workload, one adapter, successful seq16/seq64 rows, and metadata-level provenance. It does not prove seq128 support, production kernels, memory savings, adapter-switching gains, training behavior, broad PEFT coverage, upstream acceptance, or universal Transformer speedups. |
| Workload narratives | Small demos and the workload case-study artifact show how preserved provenance affects LoRA/adapters, fixed masks, per-tensor affine quantized linear modules, embedding projection, Conv1d, Torch FX capture, recovery, planning, and timing narratives. | `examples/fixed_weight_inference_demo.py`; `examples/torch_fx_frontend_demo.py`; `examples/adapter_workload_demo.py`; `examples/conv1d_workload_demo.py`; `examples/case_study_artifacts.py`; `tests/test_case_study_artifacts.py`; `docs/results/workload_case_studies.json` workflow artifact; `examples/torch_coverage_demo.py` | Full masked attention, quantized convolutional module capture, and CNN-block breadth are planned paper case-study targets until implemented with tests and artifacts. The fixed-mask row is only a fixed sparse linear map; the quantized row is packed-affine provenance and dense fallback evidence, not production integer-kernel performance; the workload case-study artifact records planner cost and memory proxies, not benchmark timings. |

## Claims Without Current Support

- Hardware-calibrated speedups: explicitly out of scope until real backend
  measurements exist.
- Universal Torch operation coverage: narrowed by `docs/torch_frontend_coverage.md`
  to supported, next, and unsupported rows.
- Broad approximation quality: narrowed to the deterministic ablation in
  `docs/benchmark_artifacts.md`.
- Production-ready recovery from dense matrices: narrowed to cheap probes,
  bounded heuristic confidence, and sample-limited validation evidence.

These boundaries are centralized in the whitepaper limitations and threats to
validity section, reflected in the support docs, and audited in
`docs/completion_audit.md`, so this matrix does not create a new follow-up
issue by itself.
